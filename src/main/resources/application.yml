server:
  port: 7205

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pg_connect

    username: username_login
    password: username_password

  jpa:
    hibernate:
      ddl-auto: update